name: Build and Release

on:
  workflow_dispatch:
    inputs:
      version_name:
        description: 'Version name (e.g. 1.0.0) — only needed when creating a release'
        required: false
        default: ''
      version_code:
        description: 'Version code — only needed when creating a release'
        required: false
        default: ''
      create_release:
        description: 'Create GitHub Release'
        required: true
        type: boolean
        default: false

env:
  KEY_STORE_PASSWORD: ${{ secrets.KEY_STORE_PASSWORD }}
  ALIAS: ${{ secrets.ALIAS }}
  KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set version info
        id: version
        run: |
          if [ "${{ inputs.create_release }}" = "true" ]; then
            echo "version_name=${{ inputs.version_name }}" >> $GITHUB_OUTPUT
            echo "version_code=${{ inputs.version_code }}" >> $GITHUB_OUTPUT
          else
            echo "version_name=manual-$(date +%s)" >> $GITHUB_OUTPUT
            echo "version_code=$(date +%s)" >> $GITHUB_OUTPUT
          fi

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build Release APK
        env:
          VERSION_NAME: ${{ steps.version.outputs.version_name }}
          VERSION_CODE: ${{ steps.version.outputs.version_code }}
        run: ./gradlew assembleRelease --stacktrace --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: LogFox-${{ steps.version.outputs.version_name }}
          compression-level: 0
          path: app/build/outputs/apk/release/LogFox-*.apk

  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: build
    if: ${{ inputs.create_release }}
    permissions:
      contents: write
    steps:
      - name: Download APK
        uses: actions/download-artifact@v4
        with:
          name: LogFox-${{ inputs.version_name }}
          path: ./apk

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ inputs.version_name }}
          name: ${{ inputs.version_name }}
          files: ./apk/*.apk
          generate_release_notes: true
